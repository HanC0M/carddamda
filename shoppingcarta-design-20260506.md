# Design: 카드담다 Session Engine First

Generated by /office-hours on 2026-05-06
Branch: unknown
Repo: local-only workspace
Status: APPROVED
Mode: Builder

## Problem Statement

TCG 카드를 자주 사는 유저는 네이버 스토어에서 원하는 카드 하나를 검색하고, 상품 페이지로 들어가고, 장바구니에 담고, 다시 돌아와 다음 카드를 검색하는 반복을 한다.

이 프로젝트의 첫 버전은 그 반복을 줄이는 것이다. 사용자는 한 화면의 왼쪽 패널에 카드명과 장수를 여러 줄로 입력한다. 오른쪽에는 각 카드명으로 검색된 결과가 묶음으로 뜬다. 사용자는 결과를 빠르게 훑고 외부 링크로 들어가 장바구니에 담는다.

핵심은 "검색" 자체보다 "여러 카드와 수량을 한 번에 처리하는 구매 세션"이다.

## What Makes This Cool

구매 리스트가 그냥 메모가 아니라 바로 행동 가능한 세션이 된다.

유저가 실제로 느끼는 감동은 이거다. "아, 이제 카드 하나씩 검색창에 붙여넣고 뒤로가기 반복 안 해도 되네."

이 제품의 첫 whoa moment는 예쁜 UI가 아니라, 10개 카드 리스트를 넣고 오른쪽에 결과가 쭉 정렬되는 순간이다.

## Constraints

- 첫 버전은 한 화면 웹앱이어야 한다.
- 네이버 스토어를 직접 바꾸는 브라우저 확장프로그램으로 시작하지 않는다.
- 검색 엔진을 새로 만드는 게 아니라, 구매 세션을 압축하는 흐름을 만든다.
- 첫 사용자상은 TCG 카드를 자주 사는 헤비 유저다.
- 장기 비전은 가격 비교, 재고, 즐겨찾기까지 가지만 첫 성공 기준은 반복 검색 시간 단축이다.
- 현재 저장소는 비어 있고 Git 저장소도 아니다. 그래서 하네스와 문서 구조를 먼저 정의해야 한다.

## Premises

1. 첫 승부는 검색 정확도 100점보다 구매 세션 압축이다.
2. 첫 버전은 독립 웹앱이어야 한다.
3. 장기 기능은 열어두되, 첫 버전의 평가는 실제 구매 세션 가속으로 한다.

## Approaches Considered

### Approach A: Thin MVP

한 화면 안에 입력, 결과, 링크 이동을 몰아넣는 가장 얇은 구조.

- 장점: 빨리 나간다. 검증이 빠르다.
- 단점: 검색 로직, 상태, 정규화가 한 군데 뭉쳐서 금방 지저분해진다.

### Approach B: Session Engine First

UI는 단순하게 유지하고, 내부는 구매 세션 중심으로 나눈다. 첫 화면은 얇지만 내부 구조는 오래 간다.

- 장점: 이후 가격 비교, 재고, 즐겨찾기, 저장 기능을 붙이기 쉽다.
- 장점: 테스트 하네스를 구조적으로 만들 수 있다.
- 단점: 초기에 파일 수와 개념 수가 조금 늘어난다.

### Approach C: Shopping Workspace

처음부터 작업 공간 개념을 넣고 선택 상태, 메모, 저장을 같이 가져가는 구조.

- 장점: 장기 제품 비전에 가장 가깝다.
- 단점: 첫 감동이 흐려지고 MVP 범위가 커진다.

## Recommended Approach

`Approach B: Session Engine First`

이유는 간단하다. 이 제품은 첫 화면은 작아도 내부는 절대 단일 컴포넌트 덩어리로 만들면 안 된다.

나중에 반드시 붙을 것들, 예를 들면 가격 비교, 검색 공급자 추가, 결과 정렬, 즐겨찾기, 세션 저장은 모두 `구매 세션`, `검색 공급자`, `결과 정규화`, `링크 액션` 경계 위에 올라간다. 이 경계를 첫날부터 잡아두는 게 맞다.

## Product Direction

### Product thesis

카드담다는 "카드 검색기"가 아니라 "카드 구매 세션 압축기"다.

사용자가 하던 일은 원래 이렇다.

1. 카드명 하나 복사
2. 네이버 스토어 검색
3. 결과 확인
4. 상품 페이지 진입
5. 장바구니 담기
6. 다시 돌아와 다음 카드 반복

카드담다의 첫 버전은 이 흐름을 아래처럼 바꾼다.

1. 카드 리스트와 장수를 한 번에 입력
2. 카드별 검색 결과를 한 화면에서 병렬로 훑음
3. 바로 링크 진입
4. 다시 앱 화면으로 돌아와 다음 카드 처리

### First version UX

- 왼쪽 패널: 카드명 + 장수 입력
- 중앙 또는 오른쪽 상단: 검색 실행, 전체 새로고침, 정렬 기준
- 오른쪽 본문: 카드명별 결과 그룹
- 각 결과 카드: 상품명, 가격, 판매처, 썸네일, 외부 이동 링크
- 상태: 로딩, 결과 없음, 부분 실패, 다시 시도

### Not in V1

- 자동 장바구니 담기
- 로그인 연동
- 가격 히스토리
- 재고 알림
- 즐겨찾기 컬렉션
- 계정 시스템

### V2 and beyond

- 가격 비교 레이어
- 검색 결과 정렬 튜닝
- 세션 저장 및 복원
- 즐겨찾기 카드 묶음
- 카드명 별칭과 세트/판본 보조 매칭
- 재고 추적

## Harness Design

하네스는 "AI가 함부로 UI만 만들고 끝내는 것"을 막는 역할을 해야 한다. 이 프로젝트는 겉보기보다 파싱과 상태 전이가 중요하다.

### Harness goals

- 구매 세션 중심 개발을 강제
- UI보다 도메인 경계를 먼저 지키게 함
- 검색 공급자 의존성을 격리
- fixture 기반 테스트를 쉽게 돌리게 함
- 이후 AGENTS가 바뀌어도 구조가 흔들리지 않게 함

### Harness layers

1. `product/`
   사용자 경험 정의. 어떤 화면과 상태가 필요한지 적는다.
2. `domain/`
   구매 세션, 카드 요청 항목, 검색 결과, 선택 상태 같은 핵심 모델.
3. `adapters/`
   네이버 스토어 검색 공급자, HTML 파서, 외부 링크 빌더.
4. `app/`
   실제 웹 UI와 상태 연결.
5. `fixtures/`
   검색 결과 샘플 HTML, 정규화 결과, 세션 입력 예시.
6. `tests/`
   파서 테스트, 세션 테스트, UI 플로우 테스트.

### Non-negotiable harness rules

- UI 컴포넌트가 검색 HTML 파싱을 직접 하면 안 된다.
- 공급자 응답은 항상 정규화 계층을 거친다.
- 세션 입력 파싱은 별도 모듈로 둔다.
- fixture 없는 파서 코드는 금지한다.
- "동작하는 데모"만 보고 merge하지 않는다. 최소한 세션 파싱과 결과 정규화 테스트가 있어야 한다.

## Execution Harness

이 프로젝트에는 "만들고 끝"이 아니라 "검수에서 GO가 나올 때까지 돌아가는 실행 하네스"가 필요하다.

원하는 동작은 명확하다.

1. 사용자가 구현 명령을 내린다.
2. 개발자 에이전트가 프론트와 백엔드/도메인 작업을 나눠 맡는다.
3. QA 에이전트가 기능, 상태, 회귀를 본다.
4. 최종 검수 에이전트가 산출물, 테스트 근거, UX 일관성을 보고 `GO` 또는 `NO_GO`를 낸다.
5. `NO_GO`면 `ralph loop`가 다시 개발 단계로 되돌아간다.

### Agent roles

- `developer-frontend`
  한 화면 구매 세션 UI, 상태 표현, 결과 그룹 레이아웃 담당
- `developer-backend`
  여기서는 전통적 서버보다 `domain + adapters + parser + normalizer` 담당
- `qa-agent`
  happy path, partial failure, empty state, parsing regressions 확인
- `release-reviewer`
  최종 산출물 검수. 이 에이전트만 `GO/NO_GO`를 선언할 수 있음

### Ralph loop contract

`ralph`는 오케스트레이터다. 직접 제품 완성 선언을 하지 않는다. 완료 조건은 항상 검수 체인을 통과하는 것이다.

루프는 아래 순서로 돈다.

1. `context snapshot` 생성
2. 작업 분해
3. 프론트/백엔드 개발 병렬 실행
4. 테스트 및 증거 수집
5. QA 검수
6. 최종 검수
7. `GO`면 종료, `NO_GO`면 결함 목록을 다음 iteration 입력으로 삼아 재시작

### Gate rules

- 개발자 에이전트는 코드만 내지 않는다. 변경 요약, 테스트 결과, 남은 리스크를 같이 낸다.
- QA 에이전트는 "대충 됨"을 금지한다. 재현 단계와 실패 상태를 적는다.
- 최종 검수 에이전트는 아래가 없으면 `GO`를 낼 수 없다.
  - 구현 요약
  - 테스트 근거
  - 알려진 제한사항
  - 사용자 입장에서의 검수 문장
- `NO_GO`가 나오면, 이유를 고친 뒤 같은 gate를 다시 통과해야 한다.

### Required artifacts per iteration

- `.omx/context/<task>.md`
  현재 작업 목표, 제약, 관련 파일, 열린 질문
- `.omx/state/ralph-progress.json`
  iteration 번호, 현재 단계, blocker, QA 결과, reviewer verdict
- `.omx/reviews/`
  QA 결과와 최종 검수 결과 저장
- `.omx/tasks/`
  프론트/백엔드 작업 분해본

### Verdict schema

최종 검수 에이전트 출력은 자유서술이 아니라 아래 형태를 따라야 한다.

```json
{
  "verdict": "GO | NO_GO",
  "summary": "one paragraph",
  "blocking_findings": [
    "..."
  ],
  "evidence": [
    "test/build/qa fact"
  ],
  "next_loop_input": [
    "only when NO_GO"
  ]
}
```

### What counts as GO

- 세션 입력에서 카드명/장수 파싱이 의도대로 동작함
- 결과 그룹이 요청 카드 기준으로 명확히 분리됨
- 로딩, 결과 없음, 부분 실패가 보임
- 적어도 fixture 기반 파서 테스트와 핵심 세션 흐름 검증이 있음
- 최종 검수 에이전트가 사용자 경험을 해치지 않는다고 판단함

### What forces NO_GO

- UI 컴포넌트 안에서 직접 파싱
- fixture 없이 파서 수정
- 카드 결과가 어느 요청 항목 소속인지 불명확함
- 빈 상태/실패 상태 누락
- 테스트 근거 없이 "manual로 확인했다"만 제출

### Harness command shape

나중에 실제 명령은 이런 식으로 맞추면 된다.

```bash
$ralph "Implement feature X for Carddamda using the project harness. Use developer-frontend, developer-backend, qa-agent, and release-reviewer. Do not finish until release-reviewer returns GO."
```

또는 팀 실행을 더 강하게 분리하고 싶으면:

```bash
$team 4 "Carddamda execution team"
```

그 다음 leader prompt에서 역할을 고정한다.

### Project-specific operating rule

카드담다에서는 `release-reviewer GO`가 곧 완료다.

코드가 돌아가는 것만으로는 완료가 아니다. `qa-agent`와 `release-reviewer`를 통과하지 못하면 계속 돈다. 이게 이 프로젝트의 ralph loop 정의다.

## Proposed Directory Structure

```text
carddamda/
  AGENTS.md
  README.md
  package.json
  docs/
    product/
      vision.md
      shopping-flows.md
      v1-scope.md
    architecture/
      system-overview.md
      search-provider-contract.md
      parsing-rules.md
    decisions/
      0001-session-engine-first.md
  src/
    app/
      layout/
      routes/
      state/
    features/
      purchase-session/
        components/
        hooks/
        state/
      search-results/
        components/
        hooks/
      card-input/
        components/
        parser/
    domain/
      session/
      card/
      search/
      result/
    adapters/
      providers/
        naver-store/
          client.ts
          parser.ts
          normalizer.ts
      linking/
    shared/
      ui/
      lib/
      types/
  fixtures/
    naver-store/
      search-html/
      normalized-results/
    sessions/
  tests/
    unit/
      domain/
      adapters/
      parser/
    integration/
      purchase-session/
    e2e/
  scripts/
    verify-fixtures.ts
    smoke-session.ts
  .omx/
    context/
    reviews/
    state/
    tasks/
```

## Why this directory structure is right

- `src/domain`은 오래 간다. UI가 바뀌어도 남는다.
- `src/adapters/providers/naver-store`는 가장 더럽고 자주 깨질 부분이다. 따로 격리해야 한다.
- `src/features/purchase-session`은 제품의 중심 흐름이다. 이 프로젝트는 페이지가 아니라 세션이 주인공이다.
- `fixtures/`를 코드 밖 최상위에 두는 이유는, 파서와 정규화 품질이 이 앱의 신뢰를 결정하기 때문이다.
- `docs/decisions`를 두는 이유는 나중에 "왜 이렇게 쪼갰지?" 같은 질문이 반드시 나오기 때문이다.

## AGENTS.md Proposal

아래는 이 프로젝트 루트에 둘 `AGENTS.md` 초안이다.

```markdown
# Carddamda Agent Guide

## Product truth

Carddamda, branded in Korean as 카드담다, is not a generic shopping search app.
It is a purchase-session accelerator for heavy TCG card buyers.

When making product or code decisions, optimize for this:
"Can a user process a multi-card purchase list faster than they can in Naver Store directly?"

If a change improves generic architecture but weakens the purchase-session flow, reject it.

## Scope rules

- V1 is a single-screen web app.
- V1 accepts multiple card names and quantities at once.
- V1 shows grouped search results by requested card.
- V1 sends users out to external product links.
- Do not add account systems, social features, notifications, or automation unless explicitly requested.

## Architecture rules

- Keep domain logic out of UI components.
- Never parse provider HTML in React components.
- Provider-specific logic lives under `src/adapters/providers/`.
- Session parsing, result normalization, and link generation must be independently testable.
- New provider integrations must implement the same normalized result contract.

## Testing rules

- Any parser change must update or validate fixtures.
- Any session input format change must include parser tests.
- Do not claim success from screenshots alone.
- Prefer fast unit and integration coverage around parsing and state transitions before E2E.

## UX rules

- Default to speed and clarity over feature count.
- The user should always know which requested card a result belongs to.
- Loading, empty, and partial-failure states are first-class UI states.
- Avoid clutter. This tool is for active buyers in the middle of a purchase session.

## Workflow rules

- Before changing structure, read `docs/product/vision.md` and the latest decision doc.
- If a proposed feature is tempting but not clearly part of V1, write it into docs instead of sneaking it into code.
- Keep changes reversible. This product is still discovering its shape.

## Execution harness rules

- For implementation tasks, default to a Ralph-style loop, not one-shot execution.
- Required roles: `developer-frontend`, `developer-backend`, `qa-agent`, `release-reviewer`.
- `release-reviewer` is the only role allowed to declare `GO`.
- If QA or reviewer returns `NO_GO`, continue the loop with those findings as the next task input.
- Do not present work to the user as complete before reviewer `GO`.
- Persist loop artifacts under `.omx/context`, `.omx/tasks`, `.omx/reviews`, and `.omx/state`.
```

## Suggested Supporting Docs

### `docs/product/vision.md`

- 누구를 위한 제품인가
- 어떤 반복 작업을 제거하는가
- V1과 V2의 경계

### `docs/product/shopping-flows.md`

- 현재 네이버 스토어에서의 구매 흐름
- 카드담다에서의 이상적 구매 흐름
- 실패 상태와 엣지 케이스

### `docs/architecture/search-provider-contract.md`

- 공급자가 반환해야 하는 필드
- 정규화 단계 정의
- 실패 시 계약

### `docs/architecture/parsing-rules.md`

- 카드명 매칭 기준
- 옵션명, 판본, 오탈자 대응 원칙
- fixture 갱신 규칙

### `docs/decisions/0001-session-engine-first.md`

- 왜 얇은 화면인데도 내부를 나누는지
- 왜 브라우저 확장이 아닌지
- 왜 `purchase session`이 제품 중심인지

## Open Questions

- 네이버 스토어 검색은 공식 API가 아니라 페이지 파싱으로 갈 가능성이 큰데, 이때 안정성과 정책 리스크를 어떻게 다룰지
- 카드명 유사어, 세트명, 판본 구분을 V1에서 어디까지 다룰지
- 외부 링크 이동 후 사용자가 다시 돌아왔을 때 어떤 상태를 유지할지
- 첫 정렬 기준을 가격, 판매량, 관련도 중 무엇으로 둘지

## Success Criteria

- 헤비 유저가 실제 구매 리스트 10개 이상으로 한 세션을 돌려볼 수 있다.
- 사용자가 "카드 하나씩 검색하는 것보다 훨씬 덜 귀찮다"라고 느낀다.
- 결과 그룹 구조가 명확해서 어떤 결과가 어떤 요청 카드에 대응되는지 헷갈리지 않는다.
- 파서와 정규화 계층은 fixture 기반으로 검증 가능하다.

## Distribution Plan

첫 버전은 웹 서비스로 배포한다.

- 배포 채널: 단일 URL
- 공유 방식: 카드 커뮤니티, 지인, 개인 사용
- CI/CD: main 배포 자동화 정도면 충분. V1에서는 과한 인프라는 필요 없다.

## Next Steps

1. 이 설계 문서를 기준으로 실제 루트 `AGENTS.md`, `docs/`, `src/` 스캐폴드를 만든다.
2. `search-provider-contract.md`를 먼저 쓴다. 이 파일이 흔들리면 나머지도 흔들린다.
3. 세션 입력 포맷을 확정한다. 예: `카드명 x 장수`.
4. fixture 3종을 먼저 만든다. 정상 검색, 결과 없음, 애매한 검색어.
5. 그 다음에 한 화면 UI를 붙인다.

## What I noticed about how you think

- "궁극적으로는 C였으면 함. 일단은 A부터 구현." 이 말이 좋았다. 욕심은 있는데, 순서를 안다.
- "카드 많이 사는 친구나 커뮤니티 유저"를 첫 반응 대상으로 잡았다. 추상적 사용자가 아니라 실제 반응 장면이 있다.
- 차별점을 "여러 카드와 수량을 한번에 다루는 구매 세션"으로 잡았다. 검색창 하나 더 만드는 함정에서 빠져나왔다.
- 첫 형태를 브라우저 확장보다 독립 웹앱으로 좁혔다. 이건 제품 검증 속도를 높이는 좋은 감각이다.

## The Assignment

실제 카드 헤비 유저 한 명의 구매 리스트를 받아라. 10개 이상 카드명과 장수로 된 진짜 리스트 하나면 충분하다.

그 리스트를 기준으로, 사용자가 네이버 스토어에서 지금 어떻게 사고 있는지 손으로 단계별로 적어라. 몇 번 검색하고, 몇 번 클릭하고, 어디서 헷갈리는지. 이 제품은 거기서 태어나야 한다.
